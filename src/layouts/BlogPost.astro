---
import type { CollectionEntry } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';
import RelatedPosts from '../components/RelatedPosts.astro';
import OptimizedImage from '../components/OptimizedImage.astro';
import '../styles/blog-post.css';
// Removed Layout import since BlogPost is already wrapped in Layout in the parent page

type Props = CollectionEntry<'blog'>['data'] & { slug: string; id: string };

const { title, description, pubDate, updatedDate, heroImage, tags, slug: slugProp, id, showDefaultCta = true } = Astro.props;

// Prefer explicit slug prop; fall back to id for brand detection
const slug = slugProp || id || '';

// Fix the heroImage path if needed
const heroImagePath = heroImage?.startsWith('/') ? heroImage : `/${heroImage}`;

// Extract keywords from the title and description for finding related posts
const titleWords = title.toLowerCase().split(/\s+/).filter(word => word.length > 4);
const descWords = description ? description.toLowerCase().split(/\s+/).filter(word => word.length > 4) : [];
const keywordTags = [...new Set([...titleWords, ...descWords, ...(tags || [])])].slice(0, 5);

// Get language from post id/slug prefix (e.g. cn/..., en/...)
const langPrefix = slug.split('/')[0];
const lang = langPrefix === 'cn' ? 'zh-CN' : 'en';

// Determine brand (Maiimg vs MaiPDF) from slug
const isMaiimg = slug?.includes('maiimg');

// Determine the target website and CTA text based on brand and language
const targetWebsite = isMaiimg
  ? (lang === 'zh-CN' ? 'https://maiimg.cn' : 'https://maiimg.com')
  : (lang === 'zh-CN' ? 'https://maipdf.cn' : 'https://maipdf.com');

const buttonText = isMaiimg
  ? (lang === 'zh-CN' ? '立即体验 Maiimg' : 'Try Maiimg Now')
  : (lang === 'zh-CN' ? '立即体验 MaiPDF' : 'Try MaiPDF Now');
---

<div class:list={['container', { 'is-maiimg': isMaiimg }]}>
  <article>
    <div class="prose">
      <div class="title">
        <h1>{title} {heroImage && <OptimizedImage class="title-icon" src={heroImagePath} alt="" loading="eager" />}</h1>
        <div class="date">
          <FormattedDate date={pubDate} />
          {
            updatedDate && (
              <div class="last-updated-on">
                Last updated on <FormattedDate date={updatedDate} />
              </div>
            )
          }
        </div>
      </div>      <div class="content-wrapper">
        <slot />
      </div>
      
      <!-- CTA Button -->
      {showDefaultCta && (
        <div class="cta-container">
          <a href={targetWebsite} target="_blank" rel="noopener noreferrer" class="cta-button">
            {buttonText}
          </a>
        </div>
      )}
      
      <!-- Show related posts with the full post ID -->
      <RelatedPosts 
        currentPost={id} 
        tags={keywordTags} 
      />
    </div>
  </article>
</div>

<!-- Mermaid script for rendering flowcharts -->
<script>
  // Function to initialize and render Mermaid diagrams
  function initMermaid() {
    // Check if Mermaid is loaded
    if (typeof mermaid === 'undefined') {
      console.warn('Mermaid library not loaded yet, retrying...');
      setTimeout(initMermaid, 100);
      return;
    }

    // Initialize Mermaid
    mermaid.initialize({ 
      startOnLoad: false, // We'll manually trigger rendering
      theme: 'default',
      flowchart: {
        useMaxWidth: true,
        htmlLabels: true,
        curve: 'basis'
      },
      securityLevel: 'loose' // Allow HTML in labels
    });
    
    // Find all mermaid code blocks and convert them
    // Check for code blocks with class containing 'mermaid' or Mermaid syntax
    const allCodeBlocks = document.querySelectorAll('pre code');
    const mermaidKeywords = ['flowchart', 'graph', 'gantt', 'sequenceDiagram', 'classDiagram', 'stateDiagram', 'erDiagram', 'journey', 'gitgraph', 'pie', 'requirement'];
    
    const mermaidBlocks = Array.from(allCodeBlocks).filter(block => {
      const classList = block.className || '';
      const parentPre = block.parentElement;
      const preClass = parentPre ? (parentPre.className || '') : '';
      const textContent = block.textContent.trim();
      
      // Check if it's marked as mermaid (language-mermaid, mermaid, etc.)
      if (classList.includes('mermaid') || preClass.includes('mermaid') || 
          classList.includes('language-mermaid')) {
        return true;
      }
      
      // Check if it starts with any Mermaid keyword
      return mermaidKeywords.some(keyword => textContent.startsWith(keyword));
    });
    
    console.log('Found mermaid blocks:', mermaidBlocks.length);
    
    mermaidBlocks.forEach((block, index) => {
      const mermaidCode = block.textContent.trim();
      if (mermaidCode) {
        const mermaidDiv = document.createElement('div');
        mermaidDiv.className = 'mermaid';
        mermaidDiv.textContent = mermaidCode;
        mermaidDiv.id = `mermaid-${index}`;
        
        // Replace the code block with the mermaid div
        const pre = block.parentElement;
        if (pre && pre.parentElement) {
          pre.parentElement.replaceChild(mermaidDiv, pre);
          console.log('Converted mermaid block:', index);
        }
      }
    });
    
    // Wait a bit for DOM to update, then render all mermaid diagrams
    setTimeout(() => {
      const mermaidElements = document.querySelectorAll('.mermaid');
      console.log('Mermaid elements to render:', mermaidElements.length);
      if (mermaidElements.length > 0) {
        try {
          mermaid.run();
        } catch (error) {
          console.error('Error rendering Mermaid diagrams:', error);
        }
      }
    }, 200);
  }

  // Load Mermaid script and initialize
  function loadMermaid() {
    const script = document.createElement('script');
    script.src = 'https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js';
    script.async = true;
    script.onload = () => {
      console.log('Mermaid library loaded');
      if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initMermaid);
      } else {
        initMermaid();
      }
    };
    script.onerror = () => {
      console.error('Failed to load Mermaid library from CDN');
    };
    document.head.appendChild(script);
  }

  // Start loading when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', loadMermaid);
  } else {
    loadMermaid();
  }
</script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    // Fix any image paths that might be incorrect
    document.querySelectorAll('img').forEach(img => {
      if (img.src.includes('../../../public/')) {
        // Fix incorrect image paths
        img.src = img.src.replace('../../../public/', '/');
        // Add lazy loading to all images that don't already have it
        if (!img.hasAttribute('loading')) {
          img.setAttribute('loading', 'lazy');
          img.setAttribute('decoding', 'async');
        }
      }
    });

    const articleImages = document.querySelectorAll('article p > img');
    
    articleImages.forEach(img => {
      // Skip images that are inside links or already in figure elements
      if (img.parentElement.tagName === 'A' || 
        img.parentElement.tagName === 'FIGURE' ||
        img.parentElement.parentElement.tagName === 'FIGURE') return;
      
      // Create figure and figcaption if alt text exists
      if (img.alt && img.alt.trim() !== '') {
        const figure = document.createElement('figure');
        const figcaption = document.createElement('figcaption');
        
        figcaption.textContent = img.alt;
        
        // Clone the image to prevent DOM manipulation issues
        const imgClone = img.cloneNode(true);
        
        // Replace the image with the figure
        img.parentNode.insertBefore(figure, img);
        figure.appendChild(imgClone);
        figure.appendChild(figcaption);
        img.remove();
      }
    });
    
    // Add smooth scrolling to anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const targetEl = document.querySelector(this.getAttribute('href'));
        if (targetEl) {
          targetEl.scrollIntoView({
            behavior: 'smooth'
          });
        }
      });
    });
  });
</script>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1.5rem;
  }
  
  .prose {
    width: 100%;
    max-width: 850px; /* More readable width */
    margin: auto;
    padding: 2rem 0;
    color: #333;
    font-family: 'Inter', sans-serif;
    font-size: 1.3rem;
    line-height: 1.8;
    position: relative;
    overflow-wrap: anywhere;
  }
  
  .title {
    margin-bottom: 3rem;
    padding-bottom: 1.5rem;
    text-align: left;
    border-bottom: 1px solid #eee;
  }
  
  .title h1 {
    margin: 0 0 0.8rem 0;
    font-weight: 800;
    font-size: 3.4rem;
    line-height: 1.2;
    letter-spacing: -0.02em;
    color: #222;
  }
  
  .title-icon {
    width: 40px;
    height: 40px;
    object-fit: cover;
    border-radius: 50%;
    margin-left: 0.5rem;
    vertical-align: middle;
  }
  
  .date {
    color: #666;
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
  }
  
  .last-updated-on {
    font-style: italic;
    font-size: 0.9rem;
    margin-top: 0.3rem;
  }
  
  /* Modern image styling with more precise control */
  article img {
    width: auto;
    max-width: 100%;
    height: auto;
    max-height: none;
    object-fit: contain;
    border-radius: 10px;
    box-shadow: 0 8px 30px rgba(2, 6, 23, 0.08);
    display: block;
    margin: 1.75rem auto;
    float: none;
    clear: none;
  }
  
  /* Add size classes for more flexibility */
  article img.tiny {
    width: 60px;
    max-height: 60px;
  }
  
  article img.small {
    width: 100px;
    max-height: 80px;
  }
  
  article img.medium {
    width: 150px;
    max-height: 120px;
  }
  
  article img.icon {
    width: 32px;
    height: 32px;
    margin: 0 0.5rem;
    float: none;
    vertical-align: middle;
    display: inline-block;
  }
  
  /* Override float for images in links */
  article a img {
    float: none;
    vertical-align: middle;
    margin: 0 0.5rem;
  }
  
  /* Clear floats after paragraphs containing images */
  article p:after {
    content: "";
    display: table;
    clear: both;
  }
  
  /* Image caption styling */
  figure {
    width: auto;
    margin: 1.75rem 0;
    float: none;
    clear: none;
  }
  
  figure.tiny {
    width: 60px;
  }
  
  figure.small {
    width: 100px;
  }
  
  figure.medium {
    width: 150px;
  }
  
  figcaption {
    width: 100%;
    font-size: 0.8rem;
    color: #666;
    font-style: italic;
    margin-top: 0.5rem;
    text-align: center;
  }
  
  /* Modern typography */
  article h2 {
    font-size: 2.2rem;
    font-weight: 700;
    margin: 2.5rem 0 1rem;
    border-bottom: 2px solid #f0f0f0;
    padding-bottom: 0.5rem;
    color: #222;
    clear: both; /* Clear image floats */
  }
  
  article h3 {
    font-size: 1.8rem;
    font-weight: 600;
    margin: 2rem 0 0.8rem;
    color: #333;
    clear: both; /* Clear image floats */
  }
  
  article p {
    margin-bottom: 1.5rem;
    font-size: 1.2rem;
    line-height: 1.7;
  }
  
  article ul, article ol {
    margin: 1.5rem 0;
    padding-left: 2rem;
    font-size: 1.2rem;
    line-height: 1.7;
  }
  
  article li {
    margin-bottom: 0.5rem;
  }
  
  /* Modern blockquote */
  article blockquote {
    border-left: 4px solid #3498db;
    background-color: #f8f9fa;
    padding: 1.2rem 1.5rem;
    margin: 1.5rem 0;
    border-radius: 0 4px 4px 0;
    position: relative;
    font-size: 1.1rem;
    color: #555;
    clear: both; /* Clear image floats */
  }
  
  article blockquote::before {
    content: "\201C";
    position: absolute;
    top: -0.5rem;
    left: -0.2rem;
    font-size: 3rem;
    color: rgba(52, 152, 219, 0.2);
    font-family: Georgia, serif;
  }
  
  /* Code blocks */
  article pre {
    background-color: #2d2d2d;
    color: #f8f8f2;
    padding: 1.2rem;
    border-radius: 6px;
    overflow-x: auto;
    margin: 2rem 0;
    font-size: 0.95rem;
    clear: both; /* Clear image floats */
  }
  
  article code {
    font-family: 'Fira Code', 'Menlo', 'Monaco', monospace;
  }
  
  article :not(pre) > code {
    background-color: #f0f0f0;
    color: #e74c3c;
    padding: 0.2rem 0.4rem;
    border-radius: 3px;
    font-size: 0.9em;
  }
  
  /* Mermaid diagram styling */
  .mermaid {
    background: white;
    padding: 1.5rem;
    border-radius: 0.5rem;
    margin: 2rem 0;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    text-align: center;
    overflow-x: auto;
  }
  
  /* Hide mermaid code blocks that haven't been converted yet */
  pre code.language-mermaid,
  pre code[class*="mermaid"] {
    display: none;
  }
  
  /* Links */
  article a {
    color: #3498db;
    text-decoration: none;
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }
    article a:hover {
    border-bottom-color: #3498db;
  }
  
  /* CTA Button Styling */
  .cta-container {
    margin: 3rem 0;
    text-align: center;
    padding: 2rem 0;
    border-top: 1px solid #eee;
    border-bottom: 1px solid #eee;
  }
  
  .cta-button {
    display: inline-block;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white !important;
    padding: 1rem 2.5rem;
    border-radius: 50px;
    font-weight: 600;
    font-size: 1.1rem;
    text-decoration: none;
    border: none;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.3);
    position: relative;
    overflow: hidden;
  }
  
  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
    border-bottom: none !important;
  }
  
  .cta-button:active {
    transform: translateY(0);
  }
  
  .cta-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
    transition: left 0.5s;
  }
  
  .cta-button:hover::before {
    left: 100%;
  }
  
  /* Better mobile responsiveness */
  @media (max-width: 768px) {
    .container {
      padding: 0 0.65rem;
    }

    .prose {
      padding: 0.5rem 0 0.75rem;
      font-size: 1rem;
      line-height: 1.7;
    }

    .title {
      margin-bottom: 1.5rem;
      padding-bottom: 0.9rem;
    }
    
    .title h1 {
      font-size: 1.85rem;
      line-height: 1.25;
    }
    
    article h2 {
      font-size: 1.35rem;
      margin: 1.5rem 0 0.75rem;
    }
    
    article h3 {
      font-size: 1.15rem;
      margin: 1.2rem 0 0.65rem;
    }
    
    article p, article ul, article ol {
      font-size: 1rem;
      line-height: 1.65;
      margin: 0.85rem 0;
    }

    article ul,
    article ol {
      padding-left: 1.2rem;
    }

    article p,
    article li,
    article a {
      word-break: break-word;
    }

    /* Keep article media readable on mobile. */
    article img,
    figure {
      width: 100%;
      max-width: 100%;
      margin: 1rem 0;
    }

    /* Prevent wide inline HTML layouts from breaking mobile viewport. */
    .content-wrapper [style*="display: grid"],
    .content-wrapper [style*="display:grid"],
    .content-wrapper [style*="grid-template-columns"] {
      grid-template-columns: 1fr !important;
      gap: 1rem !important;
    }

    .content-wrapper [style*="display: flex"],
    .content-wrapper [style*="display:flex"] {
      flex-direction: column !important;
      gap: 1rem !important;
    }

    .content-wrapper table {
      display: block;
      width: 100%;
      overflow-x: auto;
      -webkit-overflow-scrolling: touch;
      white-space: nowrap;
    }
    
    article blockquote {
      font-size: 1rem;
      padding: 0.9rem 0.95rem;
      margin: 1rem 0;
    }

    article pre {
      padding: 0.85rem;
      margin: 1rem 0;
      font-size: 0.88rem;
    }

    .cta-container {
      margin: 1.6rem 0;
      padding: 1rem 0;
    }

    .cta-button {
      width: 100%;
      max-width: 320px;
      padding: 0.85rem 1rem;
      font-size: 1rem;
    }

    /* Maiimg pages: compact mobile rhythm for heavy card/grid sections */
    .is-maiimg .content-wrapper .main-features,
    .is-maiimg .content-wrapper .tracking-info,
    .is-maiimg .content-wrapper .who-uses,
    .is-maiimg .content-wrapper .comparison,
    .is-maiimg .content-wrapper .qr-benefits,
    .is-maiimg .content-wrapper .problems-grid,
    .is-maiimg .content-wrapper .features-grid,
    .is-maiimg .content-wrapper .benefits-grid,
    .is-maiimg .content-wrapper .process-steps,
    .is-maiimg .content-wrapper .scenarios-grid,
    .is-maiimg .content-wrapper .advantages-grid,
    .is-maiimg .content-wrapper .tips-grid,
    .is-maiimg .content-wrapper .use-cases-grid {
      display: grid;
      grid-template-columns: 1fr !important;
      gap: 0.75rem !important;
      margin: 0.9rem 0 !important;
    }

    .is-maiimg .content-wrapper .feature-box,
    .is-maiimg .content-wrapper .track-card,
    .is-maiimg .content-wrapper .user-type,
    .is-maiimg .content-wrapper .vs-column,
    .is-maiimg .content-wrapper .qr-item,
    .is-maiimg .content-wrapper .problem-card,
    .is-maiimg .content-wrapper .feature-item,
    .is-maiimg .content-wrapper .scenario-card,
    .is-maiimg .content-wrapper .advantage-card,
    .is-maiimg .content-wrapper .tip-card,
    .is-maiimg .content-wrapper .testimonial-card,
    .is-maiimg .content-wrapper .start-step,
    .is-maiimg .content-wrapper .cta-box,
    .is-maiimg .content-wrapper .solution-highlight {
      padding: 0.9rem !important;
      border-radius: 10px !important;
      margin: 0 !important;
    }

    .is-maiimg .content-wrapper .feature-icon,
    .is-maiimg .content-wrapper .qr-emoji,
    .is-maiimg .content-wrapper .sec-icon {
      font-size: 1.35rem !important;
      line-height: 1;
    }

    .is-maiimg .content-wrapper .feature-text h4,
    .is-maiimg .content-wrapper .track-card h4,
    .is-maiimg .content-wrapper .user-type h4,
    .is-maiimg .content-wrapper .vs-column h4 {
      font-size: 1rem !important;
      margin-bottom: 0.45rem !important;
      line-height: 1.35;
    }

    .is-maiimg .content-wrapper .feature-text p,
    .is-maiimg .content-wrapper .track-card p,
    .is-maiimg .content-wrapper .user-type p,
    .is-maiimg .content-wrapper .vs-column li,
    .is-maiimg .content-wrapper .qr-item p,
    .is-maiimg .content-wrapper .solution-desc {
      font-size: 0.95rem !important;
      line-height: 1.55 !important;
      margin: 0.35rem 0 !important;
    }

    .is-maiimg .content-wrapper .cta-section,
    .is-maiimg .content-wrapper .limitations,
    .is-maiimg .content-wrapper .strengths {
      padding: 0.95rem !important;
      margin: 0.95rem 0 !important;
      border-radius: 10px !important;
    }

    .is-maiimg .content-wrapper .solution-button,
    .is-maiimg .content-wrapper .cta-button {
      width: 100%;
      max-width: 100%;
      text-align: center;
      padding: 0.82rem 0.95rem !important;
      border-radius: 10px !important;
      font-size: 0.96rem !important;
    }
  }

  @media (max-width: 420px) {
    .container {
      padding: 0 0.5rem;
    }

    .title h1 {
      font-size: 1.65rem;
    }
  }
</style>
